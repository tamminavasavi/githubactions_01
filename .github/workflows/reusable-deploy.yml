name: Staging

on:
  workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Set Version
        run: export MY_VERSION="v1.5.0"

      - name: Print Version
        run: echo "The version is $MY_VERSION"
